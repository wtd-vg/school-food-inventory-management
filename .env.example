# Copy file này thành .env nếu muốn đổi giá trị mặc định của Docker Compose.
# Đây chỉ là dữ liệu local, không dùng cho production.
POSTGRES_DB=schoolfood
POSTGRES_USER=schoolfood
POSTGRES_PASSWORD=schoolfood
